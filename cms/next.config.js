/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'pixinvent.com', 'www.dapurprint.co.id'],
  },
};

module.exports = nextConfig;
